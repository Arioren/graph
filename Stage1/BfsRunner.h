#pragma once
#include "Graph.h"
#include "Vertex.h"

class BfsRunner {
public:
    template <typename K, typename T>
    static void BFS(Graph<K, T>& g, Vertex<K, T>* source) {
        // TBD: complete
    }
};
