#pragma once
#include "Graph.h"
#include "Vertex.h"
#include "DfsResult.h"

class DfsRunner {
public:
    template <typename K, typename T>
    static DfsResult<K, T> DFS(Graph<K, T>& g) {
        // TBD: complete
        return DfsResult<K, T>();
    }
};
